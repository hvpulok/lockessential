'use strict';

describe('myApp.bankManager module', function() {

  beforeEach(module('myApp.bankManager'));

  describe('BankManager controller - ', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var bankManagerCtrl = $controller('BankManagerCtrl');
      expect(bankManagerCtrl).toBeDefined();
    }));

  });
});